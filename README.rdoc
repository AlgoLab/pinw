== README

* Ruby version
  2.0.0p353

* System dependencies

* Configuration

* Database creation & initialization
  run rake db:drop && rake:reset && rake:migrate

* Services (job queues, cache servers, search engines, etc.)

* Deployment instructions
